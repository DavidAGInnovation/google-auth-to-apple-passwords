This folder contains synthetic sample Google Authenticator export QR images that are safe to publish.

Use them to try the CLI locally:

```bash
node ./src/cli.js ./folder_qr
```

Do not commit real QR exports here. Keep personal screenshots in a different folder, or preserve this sample folder and point the CLI at your private path explicitly.
